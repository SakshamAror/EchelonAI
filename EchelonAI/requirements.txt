# Python dependencies for EchelonAI agent pipeline.
# Install into your venv: pip install -r requirements.txt
#
# Versions are pinned to what's known-working — yfinance/Yahoo scraping
# is version-sensitive; upgrade only after testing.

yfinance==1.3.0
tavily-python==0.7.24
python-dotenv==1.2.2
